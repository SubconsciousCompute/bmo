name: Windows

on: push

jobs:
  bmo:
    runs-on: windows-latest
    steps:
    - name: Setup Nim
      run: |
        choco install choosenim -y
        choosenim stable
        nim -v
        nimble -v
      

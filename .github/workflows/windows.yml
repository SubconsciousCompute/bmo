name: Windows

on: [push]

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2

      - name : "Build and test"
        run: |
          python -m pip install poetry -y 
          poetry install
          poetry run mypy bmo 
          poetry run pylint -E .

